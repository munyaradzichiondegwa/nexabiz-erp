{
  "name": "nexabiz",
  "version": "3.1.0",
  "private": true,
  "description": "NexaBiz – Enterprise Business Management System",
  "workspaces": ["apps/*", "packages/*", "services/*"],
  "scripts": {
    "dev":   "turbo run dev",
    "build": "turbo run build",
    "test":  "turbo run test",
    "lint":  "turbo run lint",
    "clean": "turbo run clean && rimraf node_modules"
  },
  "devDependencies": {
    "turbo":   "^1.13.0",
    "rimraf":  "^5.0.0",
    "prettier":"^3.2.0",
    "eslint":  "^8.57.0"
  },
  "engines": { "node": ">=20.0.0", "npm": ">=10.0.0" }
}